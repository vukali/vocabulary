services:
  vocab-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${VOCAB_APP_PORT:-8081}:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
