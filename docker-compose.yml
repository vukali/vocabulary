services:
  vocab-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${VOCAB_APP_PORT:-5173}:5173"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
